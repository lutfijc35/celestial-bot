services:
  celestial-bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data   # SQLite db persist
      - ./.git:/app/.git:ro  # Git history for /changelog
